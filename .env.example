# Environment Variables for Local Server
# Copy this to .env in the server directory

# Cloud Configuration
CLOUD_ENABLED=true
CLOUD_BRIDGE_URL=ws://35.209.239.164:9000/ws/gateway/
CLOUD_GATEWAY_ID=your-home-uuid-here
CLOUD_GATEWAY_SECRET=your-secret-from-pairing

# Instructions:
# 1. Get pairing code from mobile app (login -> request pairing)
# 2. Submit pairing from your local server (POST /api/gateways/complete-pairing)
# 3. Save the returned gateway_id and secret here
# 4. Restart local server
